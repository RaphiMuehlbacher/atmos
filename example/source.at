/*
enum Test {
    A,
}

impl Test {
    fn test(self: Self) -> i32 {
        match self {
            Self::A => 3,
        }
    }
}
*/

struct Foo<T> {
    foo: T,
}

impl<T> Foo<T> {
    fn foo(self: Self, t: T) {
        let a: Self = Foo { foo: t };
        /*
        let Self { foo: f } = Foo { foo: T };
        let a: T = f;
        */
    }
}

/*
fn panic() {
    return
}

enum Option<T> {
    Some(T),
    None,
}

impl<T> Option<T> {
    fn unwrap(self: Self) -> T {
        match self {
            Self::Some(inner) => inner,
            None => panic(),
        }
    }
}

fn main() {

}
*/

/*
enum Option<T> {
    Some(T),
    None
}

trait Iterator {
    type Item;

    fn next(self: &Self) -> Option<Self::Item>;
}

struct Counter {
    current: u32,
    max: u32
}

impl Counter {
    fn new(max: u32) -> Self {
        Self {
            current: 0,
            max: max,
        }
    }
}

impl Iterator for Counter {
    type Item;

    fn next(self: &Self) -> Option<Self::Item> {
        if self.current < self.max {
            let value = self.current;
            self.current += 1;
            Option::Some(value)
        } else {
            Option::None
        }
    }
}

fn main() {
    let counter = Counter::new(10);

    loop {
        match counter.next() {
            Option::Some(i) => {},
            Option::None => break,
        }
    }
}
*/
