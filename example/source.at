struct Wrapper<T> {
    value: T,
}

impl<T> Wrapper<T> {
    fn test<A>(a: A) {
        let b: A = a;
    }
}

/*
enum Option<T> {
    Some(T),
    None
}

impl<T> Option<T> {
    fn test<A>(self: Self, a: A) -> A {
        a
    }

    fn a<U>(self: Self, u: U) {
        Self::test(u);
    }

}

fn main() {
    let a: Option<i32> = Option::Some(3);
    let b: Option<bool> = Option::None;
}
*/

/*
enum Option<T> {
    Some(T),
    None
}

trait Iterator {
    type Item;

    fn next(self: &Self) -> Option<Self::Item>;
}

struct Counter {
    current: u32,
    max: u32
}

impl Counter {
    fn new(max: u32) -> Self {
        Self {
            current: 0,
            max: max,
        }
    }
}

impl Iterator for Counter {
    type Item;

    fn next(self: &Self) -> Option<Self::Item> {
        if self.current < self.max {
            let value = self.current;
            self.current += 1;
            Option::Some(value)
        } else {
            Option::None
        }
    }
}

fn main() {
    let counter = Counter::new(10);

    loop {
        match counter.next() {
            Option::Some(i) => {},
            Option::None => break,
        }
    }
}
*/
